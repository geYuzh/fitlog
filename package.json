{
  "name": "fitlog",
  "version": "1.0.0",
  "description": "FitLog \u5065\u8eab\u8bb0\u5f55",
  "scripts": {
    "build": "npx shx rm -rf www && npx shx mkdir www && npx shx cp index.html app.js manifest.json sw.js chart.umd.min.js chartjs-plugin-zoom.min.js hammer.min.js www/ && npx shx cp -r icons www/"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "shx": "^0.3.4"
  }
}
